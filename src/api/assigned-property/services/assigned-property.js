'use strict';

/**
 * assigned-property service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::assigned-property.assigned-property');
